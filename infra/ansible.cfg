[defaults]
inventory = inventory
roles_path = roles
remote_user = mkanavi
private_key_file = ~/.ssh/newvm_key
host_key_checking = False
retry_files_enabled = False
interpreter_python = auto_silent

[privilege_escalation]
become = True
become_method = sudo
become_user = root
become_ask_pass = False
