# Kokoro TTS deployment — homeserver (192.168.0.116)
# Standalone, self-contained role: single container + nginx reverse proxy with rate limiting.
# Deploy or tear down via playbooks/deploy.yml / teardown.yml

[servers]
kokoro-server ansible_host=192.168.0.116 ansible_user=mkanavi ansible_python_interpreter=/usr/bin/python3 ansible_ssh_private_key_file=~/.ssh/home-server

[docker_hosts:children]
servers
