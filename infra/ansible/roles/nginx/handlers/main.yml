---
# Nginx role - Handlers
- name: Restart nginx
  ansible.builtin.systemd:
    name: nginx
    state: restarted
    daemon_reload: yes

- name: Test nginx config
  ansible.builtin.command: nginx -t
  changed_when: false
