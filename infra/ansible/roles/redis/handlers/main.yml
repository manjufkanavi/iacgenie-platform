---
# Individual service handlers are now handled by docker-compose-generator reload handler.

- name: "No-op: restart redis (use docker-compose-generator reload instead)"
  ansible.builtin.debug:
    msg: "Container restart handled by docker-compose-generator. No separate restart needed."
