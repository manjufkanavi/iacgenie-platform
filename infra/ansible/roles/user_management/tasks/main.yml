---
# User management role — DEPRECATED (2026-08-12)
# Replaced by common role which now handles Option B: Split Roles
# (mkanavi admin user + deploy automation user).
#
# DO NOT add this role to site.yml.
# To configure users, edit roles/common/tasks/users.yml and defaults.

- name: "User Management | DEPRECATED — skipped, see common role"
  ansible.builtin.debug:
    msg: >-
      The user_management role is deprecated. User provisioning is now
      handled by the common role with Option B: Split Roles.
      See roles/common/tasks/users.yml and defaults/main.yml.
