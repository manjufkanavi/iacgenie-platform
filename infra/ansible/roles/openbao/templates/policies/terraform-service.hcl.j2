# Terraform Service Policy — read-only access to terraform KV
# Write access is reserved for the admin user only
path "terraform/kv/*" {
  capabilities = ["read", "list"]
}

path "terraform/kv" {
  capabilities = ["list"]
}
