---
# Docker Compose Generator - Env merge step
# DISABLED: We now use unified .env from OpenBao (fetch-openbao-env.py)
# Per-service .env.<service> files are no longer generated or needed.
# The unified .env is deployed directly by the deploy-env role.

- name: "Compose Gen | Skip env merge (using OpenBao unified .env)"
  ansible.builtin.debug:
    msg: "Env merge skipped. Unified .env provided by OpenBao deploy-env role."
