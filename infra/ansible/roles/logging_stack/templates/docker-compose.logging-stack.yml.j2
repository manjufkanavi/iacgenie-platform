# Logging Stack — Docker Compose (Loki + Promtail)
# Managed by Ansible
# NOTE: Loki and Promtail are defined in the unified docker-compose.yml
# This file is kept for backward compatibility — does NOT deploy conflicting services
---
# Loki and Promtail removed from this template — use unified docker-compose.yml
# This file is a no-op to prevent duplicate deployments
