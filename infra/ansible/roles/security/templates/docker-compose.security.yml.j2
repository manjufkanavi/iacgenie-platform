# =============================================================================
# Security Services — ClamAV + CrowdSec (integrated into unified compose)
# =============================================================================
# Services defined in unified docker-compose.yml as:
#   clamav-engine → security_clamav
#   crowdsec-engine → security_crowdsec
#   crowdsec-web → security_crowdsec_web
# This file is deprecated — use unified docker-compose.yml
