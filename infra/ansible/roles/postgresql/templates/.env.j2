# PostgreSQL Environment Variables
# Managed by Ansible — secrets from OpenBao
PG_ROOT_PASSWORD={{ pg_root_password | default('CHANGE_ME_IN_VAULT') }}
PG_DATA_DIR={{ pg_data_dir | default('/var/lib/postgresql/data') }}
PG_PORT={{ pg_port | default(5432) }}
