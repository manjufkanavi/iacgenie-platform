---
# =============================================================================
# nginx-config role — vars (internal)
# =============================================================================

_nginx_test_cmd: "nginx -t"
_nginx_reload_cmd: "systemctl reload nginx"
