---
- name: "No-op: restart lightserp-api (use docker-compose-generator reload instead)"
  ansible.builtin.debug:
    msg: "Container restart handled by docker-compose-generator. No separate restart needed."

- name: "No-op: restart lightserp-webui (use docker-compose-generator reload instead)"
  ansible.builtin.debug:
    msg: "Container restart handled by docker-compose-generator. No separate restart needed."
