# DEPRECATED: Use inventory/hosts.ini instead
# This file is kept for reference only.
# The YAML inventory format has been replaced with INI format for better compatibility.
# See: inventory/hosts.ini
