apiVersion: v1
kind: Service
metadata:
  name: iacgenie-backend
spec:
  selector:
    app: iacgenie-backend
  ports:
    - port: 80
      targetPort: 8000
      name: http
    - port: 9090
      targetPort: 8080
      name: metrics
  type: ClusterIP
